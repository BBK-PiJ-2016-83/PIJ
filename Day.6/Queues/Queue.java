public class Queue{
	
	public static void main(String[] args){
		
		Utilities queue = new Utilities();
		
		queue.size();
		
		queue.insert(1);
		queue.insert(2);
		queue.insert(3);
		queue.insert(4);
		queue.insert(5);
		queue.insert(6);
		queue.insert(7);
		queue.insert(8);
		queue.size();
		queue.retrieve();
		queue.size();
		queue.insert(9);
		queue.insert(10);
		queue.size();
		queue.retrieve();
		queue.retrieve();
		queue.size();
		queue.retrieve();
		queue.size();
		queue.retrieve();
		queue.retrieve();
		queue.retrieve();
		queue.retrieve();
		queue.retrieve();
		queue.retrieve();
		queue.size();
	}

}